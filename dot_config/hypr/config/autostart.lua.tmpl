-- Auto-start config
-- if you dont use UWSM add your auto start programs here, otherwise use XDG autostart https://wiki.archlinux.org/title/XDG_Autostart

hl.on("hyprland.start", function ()
    hl.exec_cmd("dbus-update-activation-environment --systemd --all")
    -- Noctalia with isolated config/state home (see environment.d/noctalia-v5.conf).
    hl.exec_cmd("env NOCTALIA_CONFIG_HOME={{ .chezmoi.homeDir }}/.config/noctalia-v5 NOCTALIA_STATE_HOME={{ .chezmoi.homeDir }}/.local/state/noctalia-v5 noctalia")
    hl.exec_cmd("xhost +SI:localuser:root")
end)
