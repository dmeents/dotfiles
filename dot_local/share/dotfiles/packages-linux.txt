# CachyOS package manifest (noctalia / v2) — minimal profile
# Installed by run_onchange_after_install-packages_linux.sh via shelly.
# One package per line; # comments and blank lines ignored. Repo+AUR mixed (shelly resolves source).
#
# ONLY genuine additions on top of the CachyOS Hyprland-Noctalia spin.
# Everything the spin installs by default (base system, kernel, desktop stack via
# cachyos-hypr-noctalia, networking/audio/bluetooth, filesystem tools) is intentionally
# omitted — it is already present on any Noctalia-spin install.

adobe-source-han-sans-cn-fonts
adobe-source-han-sans-jp-fonts
adobe-source-han-sans-kr-fonts
age
ansible
ansible-lint
beeper-v4-bin
breeze
cachyos-gaming-meta
chezmoi
chromium
claude-code
cups
dart-sass
dbeaver
discord
# Dolphin's VCS integration -- git status emblems, a sortable Version column in
# Details view, and git actions in the context menu. Enabled via [VersionControl]
# enabledPlugins=Git in dot_config/dolphinrc; without this package that key is inert.
dolphin-plugins
docker-desktop
file-roller
firefox
fuse2
ghostty
github-cli
gitkraken
gnome-keyring
# google-chrome (AUR) removed: it was the only manifest entry that never installed, and
# because the installer exits 1 on any failure it aborted every `chezmoi apply` before the
# later run_onchange_ scripts (baloo, k3s, Steam theme) could run. chromium above covers
# the Chrome-engine case.
helm
k3s-bin
kleopatra
konsole
logseq-desktop
meld
millennium
mold
noto-color-emoji-fontconfig
opendesktop-fonts
otf-monaspace-nerd
plexamp-appimage
pnpm
proton-mail-bin
proton-vpn-gtk-app
python-httpx
python-pip
python-pipx
python-uv
rtkit
ruby
rustup
sassc
socat
solaar
sops
# spotify-launcher (repo), not the AUR 'spotify': the AUR PKGBUILD verifies its source
# against Spotify's GPG key, which a non-interactive `shelly install aur -n` cannot import.
# The launcher is in cachyos-extra, so it routes through `shelly install standard` and
# self-updates the official client thereafter. See the google-chrome note above for why a
# single unattended-install failure is worth avoiding here.
spotify-launcher
starship
steam
ttf-jetbrains-mono
ttf-jetbrains-mono-nerd
typescript
vlc
watchexec
yt-dlp
zed
zellij
zen-browser-bin
zoom
